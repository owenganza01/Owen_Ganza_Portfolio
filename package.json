{
  "name": "Owen_Ganza_Portfolio-workspace",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "npm --workspace client run dev",
    "start:server": "npm --workspace server start",
    "build": "npm --workspace client run build"
  }
}
